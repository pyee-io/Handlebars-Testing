<html>

<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 2em;
            background-color: #fff;
        }

        h1 {
            color: #333;
        }

        p {
            color: #666;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            padding: 1em;
            margin: 0.5em 0;
            color: #333;
            background-color: #f9f9f9;
            border-radius: 0.5em;
        }
    </style>
</head>

<body>
    <img height="50px" src="{{image}}" alt="{{title}}" />
    <h1>{{title}}</h1>
    <p>{{body}}</p>
    <ul>
        {{#each list}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
</body>

</html>